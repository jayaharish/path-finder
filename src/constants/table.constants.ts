import { TableCellProps } from "../interface/table.interface";

export const DefaultTableItemProps: TableCellProps = {
    isEndNode: false,
    isStartNode: false,
    isVisited: false,
    isPath: false,
    weight: 0
}

export const DefaultTableCellSize = 20;